# Dropbox App key (client ID) from https://www.dropbox.com/developers/apps
# This is safe to expose in a browser build -- it is not a secret.
VITE_DROPBOX_APP_KEY=

# Path inside your Dropbox app folder where task data is stored.
VITE_DROPBOX_DATA_PATH=/opravilko-data.json
